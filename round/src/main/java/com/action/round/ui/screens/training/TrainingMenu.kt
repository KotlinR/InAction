package com.action.round.ui.screens.training

enum class TrainingMenu(val index: Int, val title: String) {
    SAVE(index = 1, title = "Save changes"),
    CLEAR(index = 2, title = "Clear exercises"),
    DELETE(index = 3, title = "Delete training"),

    SETTINGS(index = 0, title = "Setting"),
}